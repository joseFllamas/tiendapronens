{#
/**
 * @file
 * Franja de cifras sobre crema, entre el hero y el primer bloque de texto.
 */
#}
{{ attach_library('pronens/cifras') }}
<section{{ attributes.addClass('pro-cifras') }}>
  <div class="pro-cifras__grid pro-container">
    {{ content.field_items }}
  </div>
</section>
