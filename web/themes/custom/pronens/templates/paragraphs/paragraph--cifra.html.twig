{#
/**
 * @file
 * Una cifra de la franja: número grande en turquesa y etiqueta en versalitas.
 */
#}
<div{{ attributes.addClass('pro-cifra') }}>
  <div class="pro-cifra__num">{{ content.field_titulo }}</div>
  <div class="pro-cifra__label">{{ content.field_texto }}</div>
</div>
